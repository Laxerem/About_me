class Typing {
    constructor(element) {
        this.element = element
    }

    text_typing(text, delay) {
        
    }
}

class TextTyping {
    constructor(element, text_array) {
        this.element = element
        this.text_array = text_array
    }
}